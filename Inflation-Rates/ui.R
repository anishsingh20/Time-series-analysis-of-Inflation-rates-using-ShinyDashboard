library(shinydashboard)
require(shiny)
#layout of the dashboard

dashboardPage(
  #defines header
  dashboardHeader(
    title="Inflation Rates" ,
    dropdownMenu()
  ),
  #defines sidebar
  dashboardSidebar(),
  #defines bodys
  dashboardBody(
    
    
  )
)
